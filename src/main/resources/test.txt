public class MetaqListener implements  MessageListenerConcurrently{
    private static final Logger logger = LoggerFactory.getLogger(MetaqListener.class);

    @Resource
    private InteractCommonService interactCommonService;
    //@Resource
    //private ContentReadService contentReadService;
    @Resource (name = "contentServicePlatform")
    private ContentService contentService;
    @Resource
    private FlowService flowService;
	/* 这是一行注释 */
	/*************/
	/*
	多
	行
	注
	释
	*/
	这不是注释！！！ */
	
	tiancai
}